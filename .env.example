# Application
APP_NAME=BalanceHub
ENV=dev
DEBUG=true
CORS_ORIGINS=*
RATE_LIMIT=100/minute

# Security
JWT_SECRET=dev_secret_change_me
JWT_ALGORITHM=HS256
JWT_EXPIRES_MINUTES=60
WEBHOOK_SECRET_KEY=gfdmhghif38yrf9ew0jkf32

# Database (async for app)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/balancehub
# Database (sync for Alembic)
SYNC_DATABASE_URL=postgresql+psycopg2://postgres:postgres@db:5432/balancehub

# Local dev override example (without docker):
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/balancehub
# SYNC_DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/balancehub

# Seed users (for docs only; not used automatically)
DEFAULT_USER_EMAIL=user@example.com
DEFAULT_USER_PASSWORD=Password123!
DEFAULT_USER_FULL_NAME=Test User
DEFAULT_ADMIN_EMAIL=admin@example.com
DEFAULT_ADMIN_PASSWORD=Admin123!
DEFAULT_ADMIN_FULL_NAME=Admin User
